#include <stdio.h>

int main() {
    int arr[] = {1, 2, 3, 4, 5};
    int index = 2;
    int newElement = 10;

    arr[index] = newElement;

    printf("Updated Array: ");
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
